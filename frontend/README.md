# Frontend

Next.js + TypeScript + Material UI frontend for CAPSTACK.

## Setup

1. Install dependencies: `npm install`
2. Run development server: `npm run dev`
3. Build for production: `npm run build`
4. Start production server: `npm start`

## Environment Variables

- `NEXT_PUBLIC_BACKEND_URL`: Backend API URL
- `NEXT_PUBLIC_ML_URL`: ML Service URL

## Pages

- `/`: Landing page
- `/onboarding`: User onboarding form
- `/dashboard`: Main dashboard
- `/savings`: Savings management
- `/insights`: Financial insights and alerts

TODO: Add authentication, routing, and API integration.